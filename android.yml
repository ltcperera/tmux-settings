# ~/.tmuxinator/android.yml
# Development environment setup

name: dev
root: ~/

windows:    
  - shell:
      layout: main-vertical
      panes:
        - #shell
        - #shell
        - #shell
  - shell:
      - #shell
  - logcat:
      - adb logcat
