# ~/.tmuxinator/web.yml
# Development environment setup

name: dev
root: ~/

windows:    
  - shell:
      layout: main-vertical
      panes:
        - #shell
        - #shell
        - #shell
  - shell:
      - #shell
  - syslog:
      - tail -f /var/log/syslog 
