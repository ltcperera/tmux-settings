# ~/.tmuxinator/android.yml
# Development environment setup

name: dev
root: ~/

windows:    
  - shell:
      layout: main-vertical
      panes:
        - #shell
        - top
        - #shell
  - shell:
      - #shell
  - syslog:
      - tail -f /var/log/syslog 
